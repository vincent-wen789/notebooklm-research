---
title: NotebookLM Handoff Playbook
version: 2.1
updated: 2026-05-18
license: MIT
---

# NotebookLM Handoff Playbook · 调研外包工作流

> Claude × NotebookLM 协作 SOP · 把"扫源 + 综合"外包给 NotebookLM · 把"判断 + 应用 + 核对"留给 Claude。
>
> **v1 → v2 一句话 diff**：三段式 → 四段式（加 Claude 收尾核对 SOP），CI 强制 inline `[#N]` + 来源对照表 + 自检 section，数字分级核对（不再抽样靠运气）。

---

## § 1. 核心定位

### NotebookLM ≠ 调研工具，= 调研外包工序

Claude 主对话窗口跑深度调研有三个硬门槛：
1. **Token 爆炸**：扫 ≥ 10 个独立信源 + 综合 + 输出结构化报告 ≥ 50k token，主对话窗口装不下
2. **深度天花板**：WebFetch 拿单页内容，但综合 ≥ 10 页的能力跟 NotebookLM 长 context 综合不在一个量级
3. **格式锁不死**：Custom Instructions 在 NotebookLM 里是"永久预设"，Claude 主对话每次都要重新交代框架

NotebookLM 把这三个 bottleneck 解掉：自带 web search + 大量信源处理 / 长 context 综合多源 / CI 锁定输出框架 = 调研专用机。

### 分工本质

```
任务性质           适合谁           理由
────────────────────────────────────────────────
扫源 + 综合        NotebookLM       规模大 · 信源散 · 框架锁
判断 + 应用        Claude           知 owner context · 知项目状态 · 跨任务连贯
核对 + 反幻觉      Claude           NotebookLM 自己核对自己 = 鸡看鸡 · 必须外部 agent 反查
项目深度耦合       Claude           NotebookLM 不知你的代码/写作 SOP/历史决策
```

### NotebookLM 的三个系统性失真（v2 防御针对的）

1. **数字幻觉**：source A 的数字算到 source B / 转述时改写（"310 万" → "350 万"）
2. **事实/观点混淆**：source 作者的主观判断被当事实写
3. **时效模糊**：source 是 2024 年的"目前"，NotebookLM 也写成"目前"

这些不能靠"读完报告人脑校对"解决——人会漏。v2 把核对工程化到 prompt + SOP 双层：CI 强制 NotebookLM 自己附 inline `[#N]` + 来源对照表 + 自检 section，Claude 收尾按 [§ 5 阶段三 3a](#3a--claude-收尾核对-sop核心--v21-分级核对) SOP 跑**分级核对**。

---

## § 2. 四段式架构

```
①搜索大纲（Deep Research · NotebookLM 端）
   → 不设 CI，只要来源列表

②Custom Instructions（永久预设 · NotebookLM 端）
   → 变体专属框架（§9.1+）+ §9.0 硬骨架（≤500 字符 · CI 上限内）

③报告 prompt（来源审完后 · NotebookLM 端）
   → 按 CI 框架走 + 末尾两表（来源对照表 + 自检 section · 字段定义见 §6 启动姿势）

④Claude 收尾核对 SOP（Claude 端）
   → 按 § 5 阶段三 3a 跑（详见 § 5）· 分级核对 / 不重复在此展开
```

**单一事实源原则**：核对 SOP 只在 [§ 5 3a](#3a--claude-收尾核对-sop核心--v21-分级核对) 写完整版。§ 2 / § 6 §④ 仅指向 § 5，不重复内容（避免漂移）。

---

## § 3. 适用判断 · 三维度矩阵 + 规模分级

### 三维度判定

任一维度强 yes → NotebookLM；任一强 no → Claude；混合区按 §6 默认直接跑四段式（不再"拍板"）。

| 维度 | 倾向 NotebookLM ✅ | 倾向 Claude 自己 ❌ |
|---|---|---|
| **规模** | 信源 ≥ 10 个 / 报告 ≥ 3000 字 / token 估算 ≥ 50k | 单源 / 短答案 / token < 20k |
| **性质** | 扫源+综合 · 事实陈列 · 无 owner 偏好 | 判断+应用 · 策略决策 · 有 owner 偏好 |
| **时效** | 不急（1 天内 OK） | 急（现在马上要） |

### 规模分级 · 影响核对策略（v2.1 新增）

| 规模档 | 信源数 | 字数 | 估算数字数 | 核对策略 | 预估核对墙钟 |
|---|---|---|---|---|---|
| **S 小** | 10-15 | 3000-5000 | < 20 | § 5 3a 全跑 + Tier A 100% cross-ref | 15-30 min |
| **M 中** | 15-30 | 5000-10000 | 20-50 | § 5 3a 全跑 + Tier A 100% + Tier B 抽 30% | 30-60 min |
| **L 大** | > 30 | > 10000 | > 50 | 拆分多次 NotebookLM / 考虑工程化方案 | 1-3 h |

### 反向适用清单

**❌ 不要用 NotebookLM**：个人决策类（offer 比较 / 职业方向）/ 已有强 insider source / 单源短答案 / 主观判断 / 项目深度耦合 / 隐私敏感数据。

---

## § 4. 11 个分析框架变体

按任务输出"质感"分 6 大类。每类有专属 Custom Instructions 模板（详见 [§ 9 模板库](#-9-custom-instructions-模板库)）。所有变体共享 [§ 9.0 硬骨架](#-90-硬骨架所有变体必含--500-字符--ci-上限内)（强制 inline citation + 来源对照表 + 防幻觉 + 自检）。

### A · 认知扩展类（输出 = 知识地图 · 覆盖广浅）

| 变体 | 输出框架 | 典型场景 |
|---|---|---|
| **A1 领域 mapping** | 公司/工具/项目地图 + 4 维度评分 + 推荐优先级 | 求职行业公司 mapping |
| **A2 概念深研** | 5W1H + 演化路径 + 常见误解 | 想搞懂"AI Agent" 的全貌 |
| **A3 趋势调研** | 现象 + 数据 + 驱动力 + 影响 + 展望 | 某行业 6 个月趋势 |
| **A4 法规变化追踪** | 时间线 + 关键变化 + 影响面 + 应对策略 | 某法规改正案对业务影响 |

### B · 叙事/创作类（输出 = 弹药库 · 提炼深戏剧性）

| 变体 | 输出框架 | 典型场景 |
|---|---|---|
| **B1 narrative 弹药调研** | 5 弹药结构（archetypes + 决策链 + 心理曲线 + jargon + 玩家挂钩点） | 题材 narrative 创作 |
| **B2 历史复盘** | 时间线 + 关键人物 + 转折点 + 经验教训 + 当代映射 | 1929 大萧条对当代金融的启示 |

### C · 决策辅助类（输出 = 比较矩阵 · 决策导向）

| 变体 | 输出框架 | 典型场景 |
|---|---|---|
| **C1 竞品分析** | 维度评分矩阵 + 差异化点 + 各自适用场景 + 核心 takeaway | 产品竞品对比 |
| **C2 技术选型对比** | 性能 + 易用 + 生态 + 趋势 + 团队适配 | Next.js vs Astro vs SvelteKit |
| **C3 面试公司情报** | 基本面 + 文化 + 面试 pattern + 薪资范围 + 决策建议 | 面试前情报 |

### D · 市场/用户调研类（输出 = 用户画像 · marketing 视角）

| 变体 | 输出框架 | 典型场景 |
|---|---|---|
| **D1 用户画像 + 触达** | 人群细分 + 痛点 + 偏好 + 触达渠道 + 转化路径 | 产品 launch 用户调研 |

### E · 合规/法规扫盲类

| 变体 | 输出框架 | 典型场景 |
|---|---|---|
| **E1 跨法域合规扫盲** | 三层法律结构（主法/施行规则/通达）+ 责任主体 + 风险等级 + 实操路径 | 跨境进口/合规调研 |

### F · 事件/时间线类

| 变体 | 输出框架 | 典型场景 |
|---|---|---|
| **F1 事件 timeline 还原** | 触发事件 + 时间线 + 多方视角 + 影响传播 + 后续追踪 | 突发新闻快速综合 |

### 变体模板覆盖度

§ 9 当前完整模板：A1 + B1。其他 9 个变体在首次使用时 ad-hoc 写 CI，**完成后回填到 § 9**——见 § 9 末尾的回填政策。

---

## § 5. 协作分工三阶段

### 阶段一 · Claude 判断 + 出 prompt 包（5-15min）

1. 听到任务关键词 → 跑 § 3 三维度判定 + 规模分级
2. 判断 NotebookLM 适合 → 按 § 6 默认直接跑四段式（不寒暄不问拍板）
3. 按 § 4 选对应变体的 Custom Instructions 模板
4. 出四段式 prompt 包（§ 6 启动姿势模板）

### 阶段二 · owner 操作 NotebookLM（30min - 2h）

1. 新建 Notebook（命名："{项目} {变体} {时间}"）
2. Settings → Custom Instructions → 粘 § ② Custom Instructions（含 § 9.0 硬骨架）
3. Deep Research → 粘 § ① 搜索大纲 → 出来源列表
4. 审来源（10-20 min · 必跑）：勾掉低质量信源（自媒体二手转发 / 标题党 / 时效过期 / 利益冲突明显）
5. 主对话框 → 粘 § ③ 报告 prompt → 出报告
6. 报告导出 → 扔回 Claude，进入阶段三

### 阶段三 · Claude 校验 + 分析 + 入库（30-60min · v2.1 单一事实源）

#### 3a · Claude 收尾核对 SOP（核心 · v2.1 分级核对）

**适用 Claude tool**：Read, Grep, Bash (grep/wc), WebFetch
**输入**：NotebookLM 报告 markdown 路径

---

**Step 1 · 结构完整性**（Read 报告 → 检查末尾两表）

- 「来源对照表」是否存在 + 字段是否完整？无/缺 → 列「需 owner 让 NotebookLM 补」清单
- 「自检 section」是否存在 + 4 块是否齐？无/缺 → 同上
- 不强制"自动退回 NotebookLM"（Claude 无 tool 触达 NotebookLM）—— 输出缺项清单给 owner 决定补做 / 接受 / 重生成

---

**Step 2 · citation 覆盖率**（Bash grep）

```bash
# 统计 [#N] 出现次数
grep -oE '\[#[0-9]+\]' 报告.md | wc -l

# 估算每千字密度
wc -m 报告.md  # 总字数
```

- 每千字 `[#N]` ≥ 10 → ✅
- < 10 → ⚠️ flag "引用密度低，可能有未标注的推论"
- 抽样检查所有标 `[事实]` 的句子是否都带 `[#N]`

---

**Step 3 · 数字分级核对**（核心 · v2.1 分级 · 解决"全量 WebFetch 不可行"）

**Tier A · 决策关键数字**（100% 尝试 cross-ref · paywall 失败显式 reporting）

定义：金额（¥/$/€/¥）/ 百分比 / 利益分配比例 / 市场规模 / 融资金额 / 关键 KPI 数字

流程：
1. grep 报告中所有 Tier A 数字（regex 例：`[¥$€]?\d[\d,.]*\s*(万|亿|million|billion|%|％)`）
2. 每个数字 → 查紧邻的 `[#N]` → 对照「来源对照表」拿 URL
3. WebFetch URL → 比对原文
4. 误差 ≥ 5% → 标"待修正"
5. paywall / JS 渲染 / 反爬 → 标"fetch 失败 · 无法验证"（不算误差，但报 owner）

**Tier B · 时间节点**（抽样 30% · 默认信任 + 抽检）

定义：日期 / 年份 / 时间戳 / 版本号 / 时长

流程：
1. grep 时间节点
2. 随机抽 30% → WebFetch cross-ref
3. 误差 ≥ 1 个时间单位（年/月/日）→ 标"待修正"
4. 其余信任 NotebookLM（理由：日期幻觉概率比金额低）

**Tier C · 命名实体扫描**（grep 对照 · 不 fetch）

定义：人名 / 公司名 / 项目名 / 地点 / 机构名

流程：
1. grep 报告中所有专有名词（大写词 / 引号包名 / 中文双书名号）
2. 对照「来源对照表」「标题」字段
3. 报告里出现但来源对照表 0 命中 → flag "可能是 NotebookLM 推断或编造"

---

**Step 4 · 事实/观点抽检**

- 随机抽 5 条 `[事实]` 陈述 → WebFetch source URL → 验证是否真是 source 原文事实
- 随机抽 3 条 `[作者观点]` 陈述 → 验证 source 作者立场是否明确（防"业内共识"实际是某一作者的判断）

---

**Step 5 · 时效一致性**

- grep "截至 YYYY-MM-DD" → 每个日期 ≤ 对应 `[#N]` source 发布日期？
- 标 `[低置信]` 的项 → 强调"需补核实"

---

**Step 6 · 自检 section 处理**

读 NotebookLM 自检 4 块，分别决定：
- **未满足 CI 要求** → 补做 / 接受 / 让 owner 决定让 NotebookLM 补
- **未找到答案的问题** → 转 owner brain dump 或 ad-hoc 补调研
- **引用频次 top 3 source** → owner 判断是不是真复读 / 还是 source 本身就该重权重
- **内部矛盾或数据冲突** → owner 决定取舍或保留双方观点

---

**Step 7 · 输出 verdict（结构化汇总）**

```
NotebookLM 报告核对 verdict（v2.1）
═══════════════════════════════════════
【规模档】 S 小 / M 中 / L 大
【结构】 来源对照表 ✅/❌  自检 section ✅/❌
【citation 密度】 每千字 N 处（基线 ≥ 10）

【Tier A 决策数字】
  总 N 个 / 成功 cross-ref X 个 / 待修正 Y 个 / fetch 失败 Z 个

【Tier B 时间节点】
  总 N 个 / 抽样 M 个 / 通过 X 个 / 待修正 Y 个

【Tier C 命名实体】
  报告 N 个 / 对照表 M 个 / 0-命中 K 个 (待核实)

【事实/观点】 抽检通过 X/8
【时效】 通过 X / 异常 Y

【自检 4 块处理建议】
  未满足 CI: ...
  未找到答案: ...
  引用频次 top 3: ...
  内部矛盾: ...

【整体】 ✅ ship / ⚠️ 修后 ship / ❌ 退回 NotebookLM 重生成
```

---

#### 3b · 报告评估（输出完成度 + 缺口）
- 跟 CI 框架对照，每个 section 评完成度 0-100%
- 列已知缺口（owner 独有信息差 / 外部信源拿不到 / 需 brain dump 补）
- 报告 3 大亮点（直接可用的核心 insights）

#### 3c · 缺口处理
- 对每个缺口给处理方式：补调研 / brain dump / 剧本/项目创造 / 跳过
- 引导 owner brain dump 补 owner 独有信息差缺口

#### 3d · 入库（v2.1 checklist 化）

入库 checklist（缺一项 = 入库未完成）：
- ☐ 项目内：归档 markdown 到对应项目目录
- ☐ 长期资产：副本归档到知识库 / 调研目录
- ☐ 更新 frontmatter + aliases + index + log entry（如使用类似 Obsidian 工作流）
- ☐ 更新 project memory（如果是项目相关）
- ☐ **【v2.1 回填硬触发】**：本次变体在 § 9 有完整模板？无 → Claude 当场提炼 ad-hoc CI 成 § 9.{N} 模板回填 + 提交 diff 给 owner，owner ack 才算入库完成（防"主动提议"软触发衰退为 dead code）

---

## § 6. 触发原则 · 让 Claude 自动判断

### 触发关键词识别

| 类别 | 关键词 |
|---|---|
| 通用 | "深度调研" / "全面研究" / "做一个 mapping" / "梳理 X" / "弹药库" |
| 决策辅助 | "对比 X 家公司/工具/方案" / "竞品分析" / "技术选型" / "面试 N 家公司情报" |
| 行业 | "我想了解整个行业的 X" / "X 趋势" / "X 政策/法规变化" |
| 创作 | "X 这个领域弹药库" / "narrative research" / "Y 题材调研" |
| Marketing | "用户画像" / "X 用户群" / "触达 X 用户" |
| 合规 | "X 法规扫盲" / "X 跨境合规" / "进口/出口要求" |
| 事件 | "X 事件还原" / "X timeline" / "X 来龙去脉" |

### 反触发（不走 NotebookLM）

| 场景 | 处理 |
|---|---|
| "帮我看一下这个 URL" | Claude WebFetch |
| "查一下 X 是什么意思" | Claude 直接答 / 简单 search |
| "我应该选 A 还是 B" | 决策问题，Claude 引导 |
| "你觉得我这个想法怎么样" | 主观判断 |
| "你内部知识库查得到吗" | 先查本地/内部 |

### 启动姿势（默认直接跑四段式 · 不拍板）

触发关键词命中 + 三维度判定 NotebookLM 适合 → 直接进四段式输出，不寒暄不解释不给 A/B/C 让 owner 选。

**§3 "混合区"分支说明**：v1 时混合区会问 owner 拍板，v2.1 取消——直接默认跑四段式（owner 觉得不该用会主动喊停，三次同变体 stop 会触发 § 6 Failsafe 学习存 skip memory）。

### 启动话术模板（四段式）

```
[一句话锁定变体 + 规模档（S/M/L）]

§① 搜索大纲（喂 Deep Research · 不设 CI）
[关键词建议 / 重点关注 / 范围限制 / 输出"返回来源列表，每条带发布日期"]

§② Custom Instructions（粘到 Notebook Settings · 永久预设）
[§ 4 选定变体的角色 + 输出框架 + 报告结构]
+ § 9.0 硬骨架（≤500 字符 · 直接粘）

§③ 报告 prompt（来源审完后粘主对话框）
基于已收来源生成 [变体名] 报告，严格按 CI 框架走。要求：
- 每条事实/数字/引述句末必带 [#N]
- 数字（金额/比例/日期/人数）独立 [#N · 原文引述 1 句，≤50 字]，不许共用
- 每条陈述前缀 [事实]/[作者观点]/[推论] 三选一
- 时间用"截至 YYYY-MM-DD（[#N] 发布日期）"
- 置信度 [高 多源]/[中 单源]/[低 推论/冲突]

末尾附「来源对照表」（必填）：
| # | 标题 | URL | 发布日期 YYYY-MM-DD | 类型 | 权威度 | 报告内引用位置 |
- 类型：一手（原始数据/官方公告/采访/法规原文）/ 二手（媒体报道/专业分析）/ 三手（聚合/转发/UGC）
- 权威度：高（行业最权威/官方/学术）/ 中（专业媒体/资深从业者）/ 低（自媒体/匿名）
- 引用位置：列出所有引用该来源的章节号 + [#N] 出现处，具体到 § / 表 / 附录（不要写"全篇"）

末尾附「自检 section」（必填）：
## 自检 · 本次报告局限性
### 未满足的 CI 框架要求
[列出未能完全满足的 CI 要求 · 全部满足明示"无未满足项"]
### 未在 source 中找到答案的问题
[CI 要求覆盖但 source 池里没答案的问题]
### 引用频次 top 3 source 及代表段落
[列出报告中被引用次数最多的 3 个 [#N] 及其对应段落，让 owner 判断是不是复读机]
### 内部矛盾或数据冲突
[source 间矛盾，用 [#A] vs [#B] 标注双方观点，不取舍]

§④ Claude 收尾核对（你拿回报告后我按 § 5 阶段三 3a 自动跑 · 分级核对 + 结构化 verdict）
```

### Failsafe · 学习 owner 偏好

**反向偏好**：Claude 跑四段式 → owner 喊停（"不用 / 直接 WebFetch / 我自己 dump"）→ 计 1 次。同变体累积 ≥ 3 次 → Claude 主动提议存 skip memory（如 `feedback_notebooklm_skip_{变体}.md`）。

**正向偏好**：owner 主动说"这个走 NotebookLM" ≥ 3 次（同变体）→ Claude 提议存 default memory（如 `feedback_notebooklm_default_{变体}.md`）。

---

## § 7. ~~Case Studies~~（见 § 8）

> 开源版把 case studies 合并进 § 8，省去重复章节。

---

## § 8. Case Study（v1 ad-hoc · 非 v2 背书）

> 开源版仅保留 1 个公开案例，owner 自己的 narrative / mapping case 留 vault。

### Fromm 猫粮日本进口合规扫盲（E1 候选 · v1 ad-hoc）

- **背景**：跨境从美国通过转运服务把宠物食品进口到日本，需要扫盲三层法律结构（动物检疫法 + 宠物食品安全法 + 关税）
- **触发变体**：E1 跨法域合规扫盲
- **v2.1 反思**：当时走 v1 ad-hoc 模式（web_search + WebFetch 综合），未走完整 NotebookLM 四段式。如果走 v2.1，会强制「来源对照表」区分一手（监管官网通达）vs 二手（律所博客转述），现在的报告混合一手二手不分，后续核对成本不可忽略。E1 真正的首个 v2 case 待下次跨法域 sourcing 任务验证。

---

## § 9. 失败模式 + 避坑

### 通用失败模式

| 失败模式 | 症状 | v1 防御 | v2.1 强化 |
|---|---|---|---|
| **数据精准度差** | 数字/日期/金额对不上 source | 抽样 3-5 个 cross-ref | **Tier A 100% 尝试 cross-ref · paywall 失败显式 reporting（不算误差，进 verdict）+ Tier B 抽 30% / Tier C grep 对照** |
| **事实/观点混淆** | source 作者意见被当事实 | (v1 无) | **CI 强制 [事实]/[作者观点]/[推论] 前缀三选一** |
| **时效模糊** | 2024 source 写"目前" | (v1 无) | **强制"截至 YYYY-MM-DD（[#N] 发布日期）"** |
| **不确定性单档** | 只能标"待核实" | "不确定标待核实" | **三档分级 [高/中/低 置信]** |
| **CI 框架被默默跳过** | NotebookLM 自由发挥 | (v1 无) | **自检 section "未满足 CI" 强制自报** |
| **缺口黑箱** | owner 不知报告漏了啥 | (v1 无) | **自检 section "未找到答案" 强制 expose** |
| **复读机引用** | 同 source 同段引用 5 次 | (v1 无) | **自检 section "引用频次 top 3 + 代表段落"（不设硬阈值，防 NotebookLM 稀释凑数）** |
| **CI 字符爆 10k** | NotebookLM 默默截断 CI | (v1 无) | **§ 9.0 硬骨架瘦身 ≤500 字符，字段定义挪到 §③ 报告 prompt** |
| **Custom Instructions 太宽** | 报告水 · 没踩点 | 锁死框架 | (v1 同 + § 9.0 兜底) |
| **来源信噪比低** | 自媒体二手转发被收入 | 阶段二审来源 owner 必跑 | (v1 同 + 来源对照表 类型/权威度分级强制) |
| **重 thoroughness 轻 momentum** | 调研无止境 · 95% 不动笔 | "80% 就停" 原则 | (v1 同) |
| **报告里有真实名字** | 应用阶段法务 risk | "灵感→模式提取→虚构具象" | (v1 同) |
| **触发误判** | 该用时硬扛 / 反之 | failsafe 学习 | (v1 同) |
| **全量 WebFetch 不可行** | 30+ 数字 × WebFetch + paywall = 墙钟爆炸 / 伪安全感 | (v2.0 设计错误) | **v2.1 分级核对：Tier A 100% / Tier B 抽 30% / Tier C grep 对照 + fetch 失败显式标注** |

---

## § 10. Custom Instructions 模板库

### § 10.0 硬骨架（所有变体必含 · ≤500 字符 · CI 上限内）

> 注：本节在 vault 原文编号为 §9.0，开源版重排为 § 10.0。语义不变。

**直接粘到 CI 末尾**：

```
## 来源与可核对性（硬要求）
1. 事实/数字/引述句末必带 [#N]（对应文末来源对照表）
2. 数字（金额/比例/日期/人数/版本号）独立 [#N · 原文引述≤50 字]，不许多条共用
3. 每条陈述前缀 [事实]/[作者观点]/[推论] 三选一
4. 时间用"截至 YYYY-MM-DD（[#N] 发布日期）"，不用"目前/最新"
5. 置信度 [高 多源 ≥ 2]/[中 单源]/[低 推论或冲突]
6. 报告末尾必附两表（字段定义见报告 prompt）：
   - 来源对照表
   - 自检 section（4 块：未满足 CI / 未找到答案 / 引用频次 top 3 / 内部矛盾）
```

**字符统计**：实际约 320 中文字符（UTF-8 约 960 字节），叠加 B1/E1 等复杂变体框架后仍在 NotebookLM CI 10k 字符上限内有充裕余量。字段定义、类型释义、示例 → 全部放 §③ 报告 prompt（per-task ad-hoc，无字符上限）。

### § 10.1 A1 领域 mapping 模板

```
## 角色
你是一名 [行业] 招聘市场分析师 / [行业] 调研分析师。用户是 [背景描述]，正在寻找 / 评估 [对象] 的全貌。

## 输出格式
对每个符合条件的 [对象]：
- 名称 + 官网/链接 [#N]
- 一句话描述
- [关键属性 1]（评分/分类）[#N · 原文引述]
- [关键属性 2] [#N]
- [关键属性 3] [#N]
- [关键属性 4] [#N]
- 一句话推荐：为什么适合用户

## 报告结构
1. 符合条件 [对象] 列表（按 [排序维度] 排序）
2. Top 5 优先级（说明理由）
3. 持续关注的 [对象]（目前不符合但有可能）
4. 来源对照表（按硬骨架 + 报告 prompt 字段定义）
5. 自检 section（按硬骨架 4 块）

## 写作要求
- 中文输出（专有名词保留英文 + 中文括注）
- 跨来源交叉验证 ≥ 2 source 的结论标 [高置信]，单源 [中置信]

[追加 § 10.0 硬骨架]
```

### § 10.2 B1 narrative 弹药模板

```
## 角色
你是一名 [题材领域] 的 narrative research analyst。用户在做 [项目/剧本] 上的 [题材] 创作，需要 [POV 视角] 的写作弹药，不是新闻报道或法律调查。

## 输出框架（5 弹药结构）

### 1. Archetypes（[对象] 原型 3-5 类）
- 名字 + 背景画像 + 偏好 + 典型模式 + 真实案例 [#N]
- 每个原型至少 2 个真实案例锚点（项目名 + 时间 + [#N]）

### 2. 决策链（5-7 步骤化）
- 每一步：触发信号 / 内部讨论 / 外部表象 / 是否可逆 / 真实案例佐证 [#N]
- [事实] / [作者观点] / [推论] 强制区分

### 3. 心理曲线（POV-able 心理变化）
- 早期 / 中期 / 转折点 / 执行期 / 后期
- 每段至少 1 个真实案例 + source 作者心理分析 [#N · 作者观点]

### 4. 行业 Jargon 真实使用清单
- 术语 + 内部使用语境 + 外部受害者语境 + 真实使用 quote [#N · 原文引述]

### 5. 玩家代入挂钩点
- 共情前提 + 道德灰色 + 反向道德拉力
- 这部分允许 [推论] 多 · 推论依据需注明 [#N1, #N2, ...]

## 报告结构
1. 5 弹药主体
2. 真实案例库附录（10 起代表性事件 · 每起带 [#N]）
3. 来源对照表（按硬骨架 · 来源优先级：[行业最权威] > [次权威] > [媒体二手] > [社交媒体]）
4. 自检 section（按硬骨架 4 块）
5. 对剧本写作的关键 takeaway（3-5 条 · 这部分允许 [推论]）

## 写作要求
- 中文输出（专有名词保留英文 + 中文括注）
- 避免新闻播报体 + 道德评判，保持 POV 中立

[追加 § 10.0 硬骨架]
```

### § 10.3-10.11 其他变体回填政策

A2 / A3 / A4 / B2 / C1 / C2 / C3 / D1 / E1 / F1 当前**无完整模板** —— 不是占位"按需展开"，而是明确的**回填政策**：

1. **首次使用某变体**：Claude 按 § 4 该变体的"输出框架"列在 § 6 启动姿势里 ad-hoc 写 CI
2. **跑完 case 并 ship 后**：把 ad-hoc CI 提炼成模板，回填到 § 10.{对应编号}
3. **回填触发**：硬挂在 § 5 阶段三 3d 入库 checklist 里——入库不完成 = 回填提议不可跳过（不是"主动提议"软触发，防 dead code）
4. **不要在没跑过的情况下凭空写占位** —— 避免占位衰退成 dead code

**3d 入库硬触发 checklist 增项**（每次跑完阶段三必填）：
- ☐ 报告归档到项目目录
- ☐ 副本归档到知识库 / 长期调研目录
- ☐ frontmatter / aliases / index / log entry 更新
- ☐ project memory 更新（如相关）
- ☐ **【v2.1 新增 · 回填硬触发】**：本次变体在 § 10 是否有完整模板？无 → Claude 当场提炼 ad-hoc CI 成 § 10.{N} 模板回填 + 提交回填 diff 给 owner review，owner ack 后才算入库完成

当前完整模板进度：A1 ✅ / B1 ✅ / 其余 9 个待首个 case 验证后回填。

---

## § 11. ROI · 预估（接受不回填 · 出异常再 ad-hoc 调）

> ⚠️ **以下数据为预估，非 measured**。owner 实跑 case 多数为 v1，"v2.1 = X 分钟" 是 prediction。
>
> **【2026-05-18 决策】接受不系统回填**——NotebookLM case 频率约半月一次，搞 ledger + 收尾收集 5 字段 + retreat 聚合 = 维护成本 > 收益。预估数字不准 = 可接受；架构层出问题 = 才值得调。

### 预估对比（基于 M 档 · ~20 信源 / ~7000 字 / ~30 数字）

| 阶段 | v1 跑法 | v2.1 跑法（预估） |
|---|---|---|
| 收尾核对耗时 | ~1 h（人脑 grep 30 数字 / 抽样 3-5 cross-ref / 漏检风险高） | 30-45 min（Tier A 15 数字 100% cross-ref + Tier B 5 数字抽样 + Tier C grep · 分级压住） |
| 漏检风险 | 抽样 15-25% 覆盖 · 数字幻觉 / 事实观点混淆 / 时效失真 几乎裸奔 | Tier A 100% / Tier B 30% / Tier C 100% 命名实体扫描 · CI 强制自检 |
| owner 体验 | 拿到报告自己 grep 数字 · 不知道哪个数字对应哪个 source | 拿到结构化 verdict · 看 "✅/⚠️/❌ X/Y/Z" 一目了然 |

### 待验证的关键假设

- ⚠️ "Tier A 15 数字 × WebFetch ≈ 15-20 min" —— 取决于 paywall 命中率，最坏 50% fetch 失败时只能标"无法验证"
- ⚠️ "NotebookLM 真能按 § 10.0 硬骨架输出" —— 需要 case 验证 CI 不被截断、要求不被默默跳过
- ⚠️ "owner 接受'fetch 失败 · 无法验证' 不是 ship blocker" —— 看实际心理接受度

### 异常上报机制（替代"系统回填实测"）

跑 v2.1 case 时**常态不记 metric**。**仅遇以下异常**时去 log（如本地 log.md / project journal）append 一条结构化条目（≤30s）：

| 异常类型 | 触发线 | log 记什么 |
|---|---|---|
| fetch 大面积失败 | Tier A WebFetch 成功率 < 50% | 失败信源域名 + paywall 还是 404 |
| 墙钟严重超预估 | 实际核对 > 2× 预估（M 档 > 2h） | 卡哪一步、为什么 |
| CI 字符爆 | NotebookLM 截断 §10.0 硬骨架或拒收 CI | 截断点 + 现场 CI 实际字符数 |
| 数字幻觉漏检 | 收尾发现 NotebookLM 编造数字/URL | 编造内容 + 触发场景 |
| 自检 4 块缺失 ≥ 2 | NotebookLM 默默跳过 ≥ 2 块自检 | 跳过哪几块 + 推测原因 |

**聚合规则**：log 里这类条目攒到 **≥ 3 条同模式信号**（grep "notebooklm.*异常"）→ 才值得 retreat 调本节预估或 § 5 SOP。零散一两条不动。

**v2.2 触发条件**：本节预估出现**结构性错误**（不是数字偏差），如 Tier A 100% cross-ref 在 paywall 重灾区彻底跑不动 / 规模档 M/L 边界完全画错。否则 v2.1 长期维持。

---

## § 12. Changelog

### v2.1（2026-05-15 · 修复对抗性 review 发现的问题 · 二轮 review patch）

**P0 阻塞修复**：
- §④ 全量 WebFetch → **分级核对** Tier A/B/C（解决"30 数字 × WebFetch + paywall = 墙钟爆炸"）
- § 10.0 硬骨架 → **瘦身到 301 字符**（实测，解决"CI 10k 字符爆"），字段定义挪到 §③ 报告 prompt
- §④ "prompt 模板" → **SOP 重写 + 明确 tool 调度**（Read/Bash grep/WebFetch · 输出结构化 verdict）

**P1 严重修复**：
- §2/§5/§6 SOP 三处重复 → 合并到 § 5 阶段三 3a **单一事实源**
- [#N] 共用规则矛盾 → **数字独立 inline `[#N · 原文引述≤50 字]` · 不许共用**
- 复读机硬阈值 > 3 次 → **改为"引用频次 top 3 + 代表段落"**（防 NotebookLM 故意稀释凑数）
- § 11 ROI "校对工时降 75%" → **标"预估 · 待 v2.1 case 验证"**
- § 3 信源门槛 vs case 规模冲突 → **加规模分级表**（S/M/L · 影响核对策略）
- §6 启动姿势 vs §3 拍板冲突 → **取消"混合区拍板"，默认直跑** + Failsafe 兜底

**P2 polish**：
- aliases 精简为 5 个高价值
- Case Study 明标"当时走 v1 ad-hoc · 非 v2 背书"
- § 10.3-10.11 占位 → **明确回填政策 + 硬触发 checklist**（§ 5 3d 入库不完成 = 回填提议不可跳过）

**二轮 review patch（v2.1 内修，未 bump 版本号）**：
- § 9 "Tier A 100% cross-ref" 措辞 → **改为"100% 尝试 cross-ref · paywall 失败显式 reporting"**（消除与 § 11 "paywall 50% 失败" 的自我矛盾 · review B4）
- § 10.3-10.11 回填政策"主动提议" → **硬挂 § 5 3d checklist**（review B5 · 防"主动提议"软触发衰退）

**三轮 patch（2026-05-18 · 反过度工程 · v2.1 内修不 bump）**：
- § 11 "待 v2.1 case 实测回填" dead promise → **改为"接受不回填 · 出异常再 ad-hoc 调"**（NotebookLM case 半月一次，ledger + retreat 维护成本 > 收益）
- § 11 "ship 验证 case 建议" 5 字段收集 → **改为"异常上报机制"**：常态不记 metric，仅 5 类异常（fetch < 50% / 墙钟超 2× / CI 爆 / 数字幻觉 / 自检 ≥ 2 块缺失）去 log append 一行，攒 ≥ 3 条同模式信号才 retreat
- 触发条件：v2.2 仅由**结构性错误**驱动，不由"凑够数据"驱动

**二轮 review 未修复 · 留 v2.2 实测后再看**：
- Tier 分级阈值 / 规模 S/M/L 边界 / ROI 30-45min 预估 / § 10.0 瘦身后 §③ 加重 owner 工作 / § 5 3a 嵌套深 —— 都是 calibration 问题，需要 v2.1 首个真实 case 数据回填后才能 ground truth

### v2.0（2026-05-15 · 同日发布 · 已被 v2.1 取代）

- 三段式 → 四段式
- § 10.0 硬骨架（首版 · 字符过长 / SOP 与 prompt 混淆，已 v2.1 修复）
- § 5 阶段三 3a 全量核对（首版 · 全量 WebFetch 不可行，已 v2.1 改分级）
- E / F 类变体（E1 / F1）

### v1.x（2026-04-30 → 2026-05-06）

- 三段式架构
- 9 变体（A1-A4 / B1-B2 / C1-C3 / D1）
- 三维度判定
- § 5 阶段三 3a 抽样核对
- 启动姿势"默认直跑"（2026-05-06）
